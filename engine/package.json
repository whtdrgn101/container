{
  "name": "@container/engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure, deterministic game engine for Container. No I/O, no randomness — the single source of truth for game rules.",
  "license": "BSD-3-Clause",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
