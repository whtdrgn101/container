// Russian Railroads bot — the `@game-hub/game-russianrailroads/bot` subpath (Track D shape).
//
// **Stubbed until RR10.** The AI is deliberately deferred: RR10 adds a greedy baseline that decides from
// the (barely-)redacted view, ranking the single-step placements the pending-lock design exposes (see the
// ROADMAP). The subpath and file exist now so the package's four-export shape is complete from RR1; there
// is nothing to export yet.
export {};
