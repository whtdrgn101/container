{
  "name": "@container/bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI players for Container. Pure decision policies over a redacted GameView — engine = rules, bot = opinions.",
  "license": "BSD-3-Clause",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@container/engine": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
